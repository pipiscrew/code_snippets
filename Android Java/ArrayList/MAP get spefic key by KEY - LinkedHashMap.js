//http://stackoverflow.com/questions/16664808/hashmapstring-integer-search-for-part-of-an-key

	Map<String, ArrayList<CompetitionItem>> groupsItems = new LinkedHashMap<String, ArrayList<CompetitionItem>>();

	for (Entry<String, Integer> e : groupsItems.entrySet()) {
		if (e.getKey().equalsIgnoreCase(groupName)) {
	   //add to my result list
	  	}
	}