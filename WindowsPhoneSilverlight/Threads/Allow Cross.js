                // copy the data over to the main thread
                //Deployment.Current.Dispatcher.BeginInvoke(() =>
                //{
                //  //  Station = tempStation;
                //    Changed(url, urlAudioFormat);

                //    Console.WriteLine("Parsing Station Finished");
                //});