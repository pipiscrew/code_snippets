//https://developer.paypal.com/docs/classic/ipn/ht_ipn/

//strcmp — Binary safe string comparison - Note that this comparison is case sensitive.
//Returns < 0 if str1 is less than str2; > 0 if str1 is greater than str2, and 0 if they are equal.

.
.
$res = curl_exec($ch)

if (strcmp ($res, "VERIFIED") == 0) 



//example, validate that has expecting value otherwise die!
//http://x.com/CMS/checkout_info.php?type=cod
if (!isset($_GET["type"]) || ((strcmp ($_GET["type"], "cod") != 0) && (strcmp ($_GET["type"], "deposit") != 0)))
{
	die("error 0xWW2zoqhK");
}


