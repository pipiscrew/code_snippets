The key combination also works in C#. But the functionality in C# is
really only useful to re-align the code.

In VS,select the code you like then 
use the Ctrl-K + Ctrl-F combination to reformat the code