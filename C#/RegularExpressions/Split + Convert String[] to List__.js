
                string[] lines = Regex.Split(Result, "\n");

                foreach (string line in lines)
                {
                }
                
//convert Array[] to list (frm3.5) using System.Linq;
 				private List<string> reportParams;                
				reportParams = Regex.Split(Result, "\r\n").ToList();