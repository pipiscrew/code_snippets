            if (Control.ModifierKeys == Keys.Shift)
            	MessageBox.Show("with SHIFT");
            
            
              if (ModifierKeys == Keys.Control)
            	MessageBox.Show("with CTRL");