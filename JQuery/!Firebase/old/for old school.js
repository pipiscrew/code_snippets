                                for (var eventID in event) {
									var recID = event[eventID];
                                    var eventRef = new Firebase('https://csp.firebaseio.com/events/' + recID + "/info");
                                    

                                }